#!/usr/bin/env node
'use strict';

/**
 * usage-b.cjs — /bestofn 双模式用法说明脚本（Best-of-N 演练成员 B 的独立实现）
 *
 * 纯 Node，无任何外部依赖。运行时把 dsh-verifier-Pro 插件 /bestofn 命令的
 * 双模式（团队模式 + 本地模式）完整用法说明以 markdown 打印到 stdout，
 * 并以 process.exit(0) 正常结束（冒烟测试检查退出码）。
 */

const MD = [
  '# /bestofn 命令双模式用法说明（dsh-verifier-Pro）',
  '',
  '`/bestofn` 是 dsh-verifier-Pro 插件的 Best-of-N 最优方案选择协议：对同一个目标，',
  '拿到 N 份**独立完整**的候选实现，用证据链做可运行性硬筛选，再用 verifier 打分排序，',
  '最后把幸存者的优点合并成一份交付物，并通过门禁比对确认合并版本不劣于冠军。',
  '',
  '命令有两种运行模式：**团队模式**（自动派发成员生成候选）与**本地模式**（对已有产物直接评分）。',
  '',
  '---',
  '',
  '## 一、团队模式',
  '',
  '```',
  '/bestofn <goal> [N]',
  '```',
  '',
  '### 参数含义',
  '',
  '| 参数 | 含义 |',
  '| --- | --- |',
  '| `<goal>` | 目标描述，一段自然语言，说明要实现的完整任务。必须是一个**整体目标**，由每个成员各自交付一份完整实现，绝不能拆成多个方面分派给不同成员（那是任务分解，不是 Best-of-N）。 |',
  '| `[N]` | 可选，候选成员数量，默认值由插件决定（通常为 3）。`N` 越大，多样性越高，但成本和延迟也越高。 |',
  '',
  '### 执行流程',
  '',
  '1. **派发**：插件经 agent_teams 精确派出 `N` 个成员，每个成员拿到**完全相同**的任务：独立实现完整目标，把交付物保存到自己的路径并上报。多样性来自独立实现，而非分工。',
  '2. **收集**：收集 `N` 个候选产物及其路径、自描述。',
  '3. **证据链（evidence chain）**：对每个产物运行 `node scripts/evidence_chain.mjs <artifact> --summary <name>=<self-description>`。冒烟结果 `ok=false`（崩溃 / 非零退出码 / 运行期错误）的候选**当场出局**，绝不让它进入打分。',
  '4. **select 打分**：幸存者的证据块交给 `verifier select` 排序。分数差距落在噪声带时，系统自动升级为 K 轮重复评估并返回**平均后的结果**与升级元数据。',
  '5. **整合合并（integrate）**：把**所有幸存者**连同各自分数交给整合者，合成一份取各家之长的交付物。注意：只拿冠军是「排名」，不是 Best-of-N——亚军、季军往往各有强项，必须参与合并。',
  '6. **门禁（compare gate）**：把合并产物再过一遍证据链，然后 `verifier compare(合并版, 冠军)`。只有当合并版分数**不低于**冠军（噪声带内）才采用合并版；否则回退到冠军并说明原因。',
  '7. **交付**：输出最终产物路径、完整分数报告（排名 / 分数 / 升级元数据）与门禁结果。',
  '',
  '---',
  '',
  '## 二、本地模式',
  '',
  '```',
  '/bestofn --local <candidate1> <candidate2> ... [--summary name=text]... [--quick]',
  '```',
  '',
  '### 参数含义',
  '',
  '| 参数 | 含义 |',
  '| --- | --- |',
  '| `--local` | 开关：进入本地模式。不再派发成员生成候选，而是对命令行给出的**已有产物**直接跑完整流水线。 |',
  '| `<candidate1> <candidate2> ...` | 一个或多个候选产物的路径（脚本文件 / 产物文件），至少两个才谈得上「最优选择」。 |',
  '| `--summary name=text` | 可选，可重复。给名为 `name` 的候选附一句自描述 `text`，用于给 verifier 提供背景，提高打分准确性。 |',
  '| `--quick` | 可选，快速模式。跳过 / 降低整合合并与门禁比对的强度，只跑「证据链 → 打分 → 报告」的轻量路径，适合快速比较或候选数很多时。 |',
  '',
  '### 执行流程',
  '',
  '1. **证据链**：对每个候选运行 `node scripts/evidence_chain.mjs <candidate> --summary ...`。',
  '2. **崩溃出局**：`ok=false`（崩溃 / 非零退出码 / 运行期错误）的候选立即淘汰，不参与打分。',
  '3. **select 打分**：幸存者交给 `verifier select` 排序，输出排名与分数。',
  '4. **报告**：打印分数报告；若指定了 `--quick`，直接在此结束。',
  '',
  '---',
  '',
  '## 三、输出报告中可能出现的信号',
  '',
  '| 信号 | 含义 | 你应该怎么做 |',
  '| --- | --- | --- |',
  '| `signal: "flat"` | 所有候选分数完全一致，**没有任何排名区分度**。 | 绝不能直接采信其排名；用 `verifier compare` 对前两名做逐对复评，拿到真正的排序。 |',
  '| `signal: "unstable"` | 重复评估结果抖动，分数不稳定。 | 展示全部原始分数，**不要自行平均**，推荐人工复核。 |',
  '| `escalated: true, k_used, margin_before, margin_after` | 头两名分数差落在噪声带，系统自动升级为 K=3 轮、slot 交替的重复评估并返回平均结果。 | 在报告里如实带上这些升级元数据，别抹掉或四舍五入。 |',
  '| `score`（0~1） | 每条判据的 logprob 奖励分，越接近 1 越好。 | 作为排序与门禁的硬依据；close margin 交给系统自动重评。 |',
  '',
  '评分纪律：默认 `n_evaluations=1`、`pivots=2`；分数差距小（close margin）系统会自动重评并平均。',
  '',
  '---',
  '',
  '## 四、示例',
  '',
  '### 示例 1：团队模式——自动生成 N 个候选再选优',
  '',
  '```',
  '/bestofn 为 dsh-verifier-Pro 写一个零依赖的 markdown 用法说明脚本，退出码必须为 0 3',
  '```',
  '',
  '→ 插件派出 3 个成员，各自独立写脚本 → 证据链冒烟（`ok=false` 的当场出局）→ `verifier select` 打分排序 → 整合者合并 3 份幸存者 → 门禁比对合并版 vs 冠军 → 输出最终脚本与分数报告。',
  '',
  '### 示例 2：本地模式——对已有的两份产物直接比',
  '',
  '```',
  '/bestofn --local ./out/a.cjs ./out/b.cjs --summary a=零依赖同步版 --summary b=分块流式版 --quick',
  '```',
  '',
  '→ 对 `a.cjs`、`b.cjs` 各跑一次证据链（带自描述）→ 崩溃者出局 → 幸存者 `select` 打分 → 由于带 `--quick`，直接输出排名与分数报告结束，不做完整整合 / 门禁。',
  '',
  '---',
  '',
  '## 五、铁律提醒',
  '',
  '- Best-of-N 的候选必须是**同一目标的完整独立实现**，禁止按方面拆分给不同成员。',
  '- 证据链 `ok=false` = 当场出局，无论自述多好。',
  '- 打分信**实测产物**，不信成员自夸（「自报功能特性」不算证据）。',
  '- `flat` 无排序信号，必须用 compare 复核；`unstable` 展示原始分并建议人工复核。',
  '- 合并版必须过门禁（不低于冠军，噪声带内）才可取代冠军。',
].join('\n');

// 打印完整说明
console.log(MD);
console.log(''); // 末尾留一行空白，便于阅读

// 冒烟测试要求：以 0 退出码正常结束
process.exit(0);
