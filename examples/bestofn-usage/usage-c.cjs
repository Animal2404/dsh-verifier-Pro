#!/usr/bin/env node
'use strict';

// ============================================================================
// usage-c.cjs — dsh-verifier-Pro 插件 /bestofn 命令双模式完整用法说明
//
// 纯 Node.js，无任何外部依赖。运行时将整份 Markdown 用法说明打印到 stdout，
// 并以 process.exit(0) 正常结束（冒烟测试检查退出码）。
//
// 覆盖内容：
//   1) 团队模式   /bestofn <goal> [N]
//   2) 本地模式   /bestofn --local <candidate1> <candidate2> ... [--summary name=text]... [--quick]
//   3) 参数含义、执行流程、输出信号（flat / escalated 元数据）
//   4) 具体示例
// ============================================================================

const DOC = `# /bestofn 命令双模式完整用法说明

\`/bestofn\` 是 **dsh-verifier-Pro** 插件的 Best-of-N 最优选择协议命令。它从 N 个独立实现的候选中，用「证据链（evidence chain）+ LLM 作为验证器（verifier）打分」的方式选出最优解，并把所有幸存候选的最佳部分整合成一份最终交付物。

命令支持 **两种模式**：**团队模式**（自动派生成员并并行实现）与 **本地模式**（对已有产物直接打分）。

---

## 模式一：团队模式（默认）

\`\`\`
/bestofn <goal> [N]
\`\`\`

### 参数说明

| 参数 | 必填 | 含义 |
| --- | --- | --- |
| \`<goal>\` | 是 | 要实现的目标/任务的完整描述。所有成员都会被派发**同一个** goal，各自独立完成一整份实现（绝不按方面拆任务）。 |
| \`[N]\` | 否 | 独立候选实现的数量，默认 4。N 越大，探索空间越广，但耗时与 token 成本越高。 |

### 执行流程

1. **派生成员**：派 N 个团队成员，每个成员拿到**完全相同**的目标，独立交付一份完整实现，并把产物保存到各自的路径后上报。
2. **证据链**：对每个产物运行 \`node scripts/evidence_chain.mjs <artifact> --summary <name>=<self-description>\`。冒烟结果为 \`ok=false\`（崩溃、退出码非 0、运行时错误）的候选**当场淘汰**，绝不进入打分。
3. **打分排序**：所有幸存候选的完整内容进入 verifier \`select\`（自适应 K，会自动处理接近的分数差）。\`flat\` 结果不携带排序信号，需对前两名单独 \`compare\` 确认。
4. **整合合并**：把**所有**幸存候选连同各自分数交给整合者（专职成员或队长单独一轮），合成一份融合各家优点的新交付物——不是简单取冠军。
5. **门禁**：对合并产物再跑一遍证据链，然后用 verifier \`compare(merged, champion)\` 对比。合并版只有在分数不低于冠军（噪声范围内）时才被采纳，否则回退到冠军并说明原因。
6. **交付**：输出最终产物路径、完整分数报告（排名、分数、升级元数据）与门禁结果。

---

## 模式二：本地模式（对已有产物打分）

\`\`\`
/bestofn --local <candidate1> <candidate2> ... [--summary name=text]... [--quick]
\`\`\`

### 参数说明

| 参数 | 必填 | 含义 |
| --- | --- | --- |
| \`--local\` | 是 | 切换到本地模式，不派成员，直接对命令行给出的已有产物文件打分。 |
| \`<candidate1> <candidate2> ...\` | 是 | 候选产物文件路径，至少 2 个，空格分隔。 |
| \`--summary name=text\` | 否 | 为某个候选附加一句自描述（如 \`--summary a=带重试的HTTP客户端\`），可重复多次，辅助 verifier 理解候选意图。 |
| \`--quick\` | 否 | 快速模式：降低验证开销（例如 \`n_evaluations=1\`、更少 pivot），适合候选多、时间紧的场景。 |

### 执行流程

1. **证据链**：对每个候选运行 \`node scripts/evidence_chain.mjs <candidate> --summary <name>=<text>\`。
2. **崩溃出局**：\`ok=false\` 的候选（冒烟失败、退出码非 0、运行时崩溃）直接淘汰，剩余幸存者进入打分。
3. **打分**：verifier \`select\` 对幸存候选排序；若结果 \`flat\`（无区分信号），对前两名补一次 \`compare\` 确认。
4. **报告**：输出候选排名、各项分数、升级元数据与门禁建议（本地模式不自动整合，只给出选择结论）。

---

## 输出报告中的信号

- **flat**：所有候选得分完全相同，\`signal:"flat"\` 表示本次打分**没有排序信号**——绝不能照搬它的排名，必须用 \`compare\` 单独确认前两名后再做决策。
- **escalated**：当某对候选的分数差落在噪声带内时，系统会触发重评（K=3、槽位交替），并返回 \`escalation\` 元数据：\`escalated\`（是否升级）、\`k_used\`（重评使用的 K 值）、\`margin_before\`（重评前分数差）、\`margin_after\`（重评后分数差）。请把这份元数据如实写进报告，不要自行平均或四舍五入分数。
- **unstable**：若 \`signal:"unstable"\`，呈现所有原始分数并建议人工复核，绝不可自行取平均。
- 分数区间为 \`[0,1]\` 的 logprob 奖励。报告分数时**不得编造或抹掉**任何数字。

---

## 示例

### 示例 1：团队模式（默认 4 个候选）

\`\`\`
/bestofn 写一个用法说明脚本 tmp_articles/usage/usage-a.cjs 5
\`\`\`

> 派 5 个成员各自独立实现同一份脚本 → 对 5 个产物跑证据链（冒烟失败的淘汰）→ verifier select 排序 → 整合所有幸存者的优点 → 对合并版跑证据链并用 compare 对比冠军 → 输出最终产物路径与完整分数报告。

### 示例 2：本地模式，对两份既有产物打分并开启快速模式

\`\`\`
/bestofn --local ./build/app-a.cjs ./build/app-b.cjs --summary app-a=带重试的HTTP客户端 --summary app-b=流式解析版 --quick
\`\`\`

> 对 app-a.cjs 与 app-b.cjs 各跑证据链，冒烟失败（崩溃/退出码非 0）者当场出局；幸存者进入 verifier select 打分；若分数差落在噪声带，自动升级重评并返回 escalated 元数据；最终报告给出排名、分数与门禁建议。

---

*提示：证据链冒烟（\`ok=false\` 即淘汰）是所有模式的硬门槛；打分与整合只对幸存候选进行。*
`;

console.log(DOC);
process.exit(0);
