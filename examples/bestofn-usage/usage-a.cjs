#!/usr/bin/env node
'use strict';

/**
 * usage-a.cjs — /bestofn 双模式用法说明脚本
 *
 * 纯 Node.js、无任何外部依赖。运行后在 stdout 打印 dsh-verifier-brain
 * 插件 /bestofn 命令双模式（团队模式 / 本地模式）的完整用法说明，
 * 并以 process.exit(0) 正常结束（冒烟测试检查退出码）。
 *
 * 这是 Best-of-N 演练中成员 usage-a 的独立实现。
 */

const doc = String.raw`
# /bestofn — Best-of-N 最优选择协议（双模式用法说明）

\`/bestofn\` 是 dsh-verifier-brain 插件的核心命令：在 N 个独立候选实现中，
用"证据链 + verifier 打分"挑选最优者，并把落选者的长处合并进最终交付物。
本说明覆盖两种运行模式：**团队模式**（队长派发成员实现）与**本地模式**（对已有产物打分）。

---

## 模式一：团队模式

\`\`\`
/bestofn <goal> [N]
\`\`\`

### 参数

| 参数  | 必填 | 含义 |
|-------|------|------|
| \`<goal>\` | 是 | 目标描述。一句话说清"要交付什么"，同一目标会原样派发给 N 个成员 |
| \`[N]\`    | 否 | 候选成员数，默认 3。每个成员收到**完全相同**的完整任务（不是切分！） |

> 关键原则：团队模式派发的是 N 份**完整独立实现**。多样性来自成员各自的
> 实现方式，而不是把任务拆成 N 块每人做一块——切分会让候选失去可比性，
> 破坏排名的意义。

### 执行流程（六步）

1. **派发**：队长 spawn 恰好 N 个成员（agent_teams），每个成员执行同一任务：
   交付一个**完整**的独立实现，保存产物并报告路径。
2. **收集**：收集 N 份产物文件路径。
3. **证据链（evidence chain）**：对每份产物运行
   \`node scripts/evidence_chain.mjs <artifact> --summary <name>=<self-description>\`。
   冒烟结果 \`ok=false\`（崩溃 / 非零退出 / 运行时报错）的候选**当场出局**，
   绝不进入打分阶段。
4. **select 排名**：幸存者进入 \`verifier select\` 打分排名（自适应 K 自动处理
   接近的分数差；见下文"报告信号"）。
5. **整合合并（integrate）**：把**所有幸存者**连同各自分数交给整合者（成员或
   队长另起一轮），合并各自优点成一份交付物——只取冠军是"排名"不是 Best-of-N。
6. **compare 门禁（gate）**：合并产物再次过证据链，然后 \`verifier compare(merged, champion)\`。
   仅当合并版分数 ≥ 冠军（在噪声范围内）才采纳合并版；否则回退到冠军并说明原因。

### 示例

\`\`\`
/bestofn 写一个 /bestofn 双模式用法说明脚本 3
\`\`\`

→ 3 个成员各自独立写脚本 → 3 份产物过证据链 → 幸存者 select 排名 →
整合者合并三份的优点成最终脚本 → compare 门禁把关后交付最终路径与完整分数报告。

---

## 模式二：本地模式

\`\`\`
/bestofn --local <candidate1> <candidate2> ... [--summary name=text]... [--quick]
\`\`\`

### 参数

| 参数 | 必填 | 含义 |
|------|------|------|
| \`--local\` | 是 | 切换到本地模式：不派成员，对**已存在**的候选产物打分 |
| \`<candidate1> <candidate2> ...\` | 是 | 至少 2 个候选产物路径（文件或可执行脚本） |
| \`--summary name=text\` | 否 | 给某个候选附上作者自述（可重复）。供证据链与打分参考 |
| \`--quick\` | 否 | 快速模式：降低打分轮数 / 采样，适合候选多、时间紧的场景 |

### 执行流程

1. **证据链**：对每个候选运行证据链脚本（冒烟 + 自述摘要）。
2. **崩溃出局**：\`ok=false\` 的候选当场淘汰，不参与打分。
3. **select 报告**：幸存候选进入 \`verifier select\`，产出排名、分数与
   信号元数据，最终给出推荐冠军与完整分数报告。

### 示例

\`\`\`
/bestofn --local build/a.cjs build/b.cjs build/c.cjs \
  --summary a=事件循环优先 --summary b=缓存分层 --quick
\`\`\`

→ 三个候选都过证据链 → 崩溃者出局 → select 给出排名与推荐。

\`\`\`
/bestofn --local dist/v1.mjs dist/v2.mjs dist/v3.mjs
\`\`\`

→ 标准模式：不做快速采样，逐个候选完整打分后排名。

---

## 输出报告中的信号（signal / 元数据）

打分报告会附带信号与元数据，解读规则如下：

- **\`flat\`**：所有候选得分相同——排名**无信号**，不可按原排名采纳；
  必须用 \`compare\` 对前两名做两两确认后再决定。
- **\`escalated\`**：某两名的分数差落入噪声带，系统自动重评
  （K=3、槽位交替），返回平均后的结果。报告会带 \`k_used / margin_before /
  margin_after\` 元数据——请**如实转述**这些数字，不要四舍五入或编造。
- **\`unstable\`**：重评后信号仍不稳定——请展示全部原始分数并建议人工复核，
  绝不要自己平均。
- **分数范围**：verifier 分数在 \`[0,1]\`，越低越差。

### 证据纪律

- 打分依据的是**可验证产物**（冒烟测试结果、运行错误数、产物中的硬事实），
  不是作者的自述。候选在运行期崩溃 = 直接淘汰，无论它声称多好。
- 合并后的门禁（compare merged vs champion）若失败，回退冠军并说明原因，
  分数照实报告。

---

## 速查

| 想做什么 | 用哪个 |
|----------|--------|
| 让 N 个成员从零实现同一目标并挑最优 | \`/bestofn <goal> [N]\` |
| 对手头已有的若干产物打分排名 | \`/bestofn --local ...\` |
| 打分前对产物做冒烟验证 | 证据链（evidence_chain） |
| 判定"合并版 vs 冠军" | \`compare\`（门禁） |
| 快速粗筛大量候选 | 加 \`--quick\` |

本说明由成员 usage-a 独立生成（Best-of-N 演练产物）。
`;

process.stdout.write(doc + '\n');
process.exit(0);
