#!/usr/bin/env node
'use strict';

/**
 * merged-usage.cjs — /bestofn 双模式用法说明（Best-of-N 整合合并版）
 *
 * 由三份独立成员实现（usage-a / usage-b / usage-c）整合合并而成：
 *   - usage-a：六步流程结构、4 个示例、速查表
 *   - usage-b：七步流程（含交付）、本地模式参数表、字符串数组规避反引号冲突、评分纪律
 *   - usage-c：整合合并/门禁/交付六步、本地模式参数表、escalated 元数据字段名
 *
 * 纯 Node，无任何外部依赖。运行后在 stdout 打印完整 markdown 用法说明，
 * 并以 process.exit(0) 正常结束（冒烟测试检查退出码）。
 *
 * 技术取舍：正文用字符串数组 join('\n') 而非模板字面量 —— 单引号字符串中的
 * 反引号是普通字符，完全规避 markdown 反引号与 JS 模板字符串的转义冲突。
 */

const MD = [
  '# /bestofn 命令双模式完整用法说明（dsh-verifier-Pro）',
  '',
  '`/bestofn` 是 **dsh-verifier-Pro** 插件的 Best-of-N 最优选择协议命令：对同一个目标，',
  '拿到 N 份**独立完整**的候选实现，用「证据链（evidence chain）冒烟硬筛选 + verifier 打分」',
  '选出最优解，并把所有幸存候选的最佳部分整合成一份最终交付物。',
  '',
  '命令支持 **两种模式**：**团队模式**（自动派生成员并行实现）与 **本地模式**（对已有产物直接打分）。',
  '',
  '---',
  '',
  '## 一、团队模式（默认）',
  '',
  '```',
  '/bestofn <goal> [N]',
  '```',
  '',
  '### 参数含义',
  '',
  '| 参数 | 必填 | 含义 |',
  '| --- | --- | --- |',
  '| `<goal>` | 是 | 目标/任务的完整描述。所有成员都会被派发**同一个** goal，各自独立完成**一整份完整实现**——绝不能按方面拆成多块分派给不同成员（那是任务分解，不是 Best-of-N，会破坏排名的可比性）。 |',
  '| `[N]` | 否 | 独立候选实现的数量，默认值由插件决定（通常为 3）。`N` 越大，探索空间越广、多样性越高，但耗时与 token 成本也越高。 |',
  '',
  '> 关键原则：团队模式派发的是 N 份**完整独立实现**。多样性来自成员各自的实现方式，',
  '> 而不是把任务拆成 N 块每人做一块——切分会让候选失去可比性，破坏排名的意义。',
  '',
  '### 执行流程（七步）',
  '',
  '1. **派发**：插件经 agent_teams 精确派出 `N` 个成员，每个成员拿到**完全相同**的任务：独立实现完整目标，把交付物保存到自己的路径并上报。',
  '2. **收集**：收集 `N` 个候选产物及其路径、自描述。',
  '3. **证据链（evidence chain）**：对每个产物运行 `node scripts/evidence_chain.mjs <artifact> --summary <name>=<self-description>`。冒烟结果 `ok=false`（崩溃 / 非零退出码 / 运行期错误）的候选**当场出局**，绝不进入打分。',
  '4. **select 打分**：幸存者的证据块交给 `verifier select` 排序。分数差距落在噪声带时，系统自动升级为 K 轮重复评估并返回**平均后的结果**与升级元数据（见「报告信号」）。',
  '5. **整合合并（integrate）**：把**所有幸存者**连同各自分数交给整合者（专职成员或队长另起一轮），合成一份取各家之长的交付物。注意：只取冠军是「排名」，不是 Best-of-N——亚军、季军往往各有强项，必须参与合并。',
  '6. **门禁（compare gate）**：把合并产物再过一遍证据链，然后 `verifier compare(合并版, 冠军)`。只有当合并版分数**不低于**冠军（噪声带内）才采用合并版；否则回退到冠军并说明原因。',
  '7. **交付**：输出最终产物路径、完整分数报告（排名 / 分数 / 升级元数据）与门禁结果。',
  '',
  '---',
  '',
  '## 二、本地模式（对已有产物打分）',
  '',
  '```',
  '/bestofn --local <candidate1> <candidate2> ... [--summary name=text]... [--quick]',
  '```',
  '',
  '### 参数含义',
  '',
  '| 参数 | 必填 | 含义 |',
  '| --- | --- | --- |',
  '| `--local` | 是 | 切换到本地模式：不再派发成员生成候选，而是对命令行给出的**已有产物**直接跑完整流水线。 |',
  '| `<candidate1> <candidate2> ...` | 是 | 候选产物的路径（脚本文件 / 产物文件），至少 2 个才谈得上「最优选择」，空格分隔。 |',
  '| `--summary name=text` | 否 | 为某个候选附加一句自描述（可重复多次），如 `--summary a=带重试的HTTP客户端`，供证据链与 verifier 理解候选意图。 |',
  '| `--quick` | 否 | 快速模式：降低打分开销（如 `n_evaluations=1`、更少 pivot），只跑「证据链 → 打分 → 报告」的轻量路径，适合候选多、时间紧的场景。 |',
  '',
  '### 执行流程',
  '',
  '1. **证据链**：对每个候选运行 `node scripts/evidence_chain.mjs <candidate> --summary <name>=<text>`。',
  '2. **崩溃出局**：`ok=false`（冒烟失败 / 退出码非 0 / 运行时崩溃）的候选立即淘汰，不参与打分。',
  '3. **select 打分**：幸存者交给 `verifier select` 排序，输出排名与分数；若结果 `flat`，对前两名补一次 `compare` 确认。',
  '4. **报告**：打印分数报告；若指定了 `--quick`，直接在此结束（本地模式不自动整合，只给出选择结论与门禁建议）。',
  '',
  '---',
  '',
  '## 三、输出报告中的信号',
  '',
  '| 信号 | 含义 | 你应该怎么做 |',
  '| --- | --- | --- |',
  '| `signal: "flat"` | 所有候选分数完全一致，本次打分**没有排序信号**。 | 绝不能照搬其排名；用 `verifier compare` 对前两名做逐对复评，拿到真正的排序后再决策。 |',
  '| `escalated: true, k_used, margin_before, margin_after` | 某对候选的分数差落在噪声带内，系统自动升级为 K=3 轮、槽位（slot）交替的重复评估，并返回平均后的结果。 | 把这份升级元数据**如实**写进报告：`k_used`（重评所用 K 值）、`margin_before`（重评前分数差）、`margin_after`（重评后分数差）——不要自行平均、抹掉或四舍五入。 |',
  '| `signal: "unstable"` | 重复评估结果抖动，分数不稳定。 | 展示全部原始分数并建议人工复核，**绝不可自行取平均**。 |',
  '| `score`（0~1） | 每条判据的 logprob 奖励分，越接近 1 越好。 | 作为排序与门禁的硬依据；报告分数时不得编造或抹掉任何数字。 |',
  '',
  '**评分纪律**：默认 `n_evaluations=1`、`pivots=2`；分数差小（close margin）时系统会自动重评并平均。',
  '',
  '---',
  '',
  '## 四、示例',
  '',
  '### 示例 1：团队模式——自动生成 N 个候选再选优',
  '',
  '```',
  '/bestofn 为 dsh-verifier-Pro 写一个零依赖的 markdown 用法说明脚本，退出码必须为 0 3',
  '```',
  '',
  '→ 插件派出 3 个成员各自独立写脚本 → 证据链冒烟（`ok=false` 的当场出局）→ `verifier select` 打分排序 → 整合者合并 3 份幸存者 → 门禁比对合并版 vs 冠军 → 输出最终脚本与完整分数报告。',
  '',
  '### 示例 2：团队模式——省略 N 用默认值',
  '',
  '```',
  '/bestofn 写一个用法说明脚本 tmp_articles/usage/usage-a.cjs',
  '```',
  '',
  '→ 使用插件默认候选数；流程与示例 1 相同，`N` 缺省。',
  '',
  '### 示例 3：本地模式——对已有产物打分，带自述与快速模式',
  '',
  '```',
  '/bestofn --local ./out/a.cjs ./out/b.cjs --summary a=零依赖同步版 --summary b=分块流式版 --quick',
  '```',
  '',
  '→ 对 `a.cjs`、`b.cjs` 各跑一次证据链（带自描述）→ 崩溃者出局 → 幸存者 `select` 打分 → 因带 `--quick`，直接输出排名与分数报告结束，不做完整整合 / 门禁。',
  '',
  '### 示例 4：本地模式——标准模式完整打分',
  '',
  '```',
  '/bestofn --local ./build/app-a.cjs ./build/app-b.cjs ./build/app-c.cjs',
  '```',
  '',
  '→ 不做快速采样，对三个候选逐个完整打分；若分数差落入噪声带，自动升级重评并返回 escalated 元数据；最终报告给出排名、分数与门禁建议。',
  '',
  '---',
  '',
  '## 五、速查表',
  '',
  '| 想做什么 | 用哪个 |',
  '| --- | --- |',
  '| 让 N 个成员从零实现同一目标并挑最优 | `/bestofn <goal> [N]` |',
  '| 对手头已有的若干产物打分排名 | `/bestofn --local <c1> <c2> ...` |',
  '| 打分前对产物做冒烟验证 | 证据链 `node scripts/evidence_chain.mjs ...` |',
  '| 判定「合并版 vs 冠军」 | `verifier compare`（门禁） |',
  '| 快速粗筛大量候选 | 本地模式加 `--quick` |',
  '',
  '---',
  '',
  '## 六、铁律提醒',
  '',
  '- Best-of-N 的候选必须是**同一目标的完整独立实现**，禁止按方面拆分给不同成员。',
  '- 证据链冒烟 `ok=false`（崩溃 / 退出码非 0 / 运行期错误）= 当场出局，无论自述多好——这是所有模式的硬门槛。',
  '- 打分信**实测产物**，不信成员自夸（「自报功能特性」不算证据）。',
  '- `flat` 无排序信号，必须用 compare 复核；`unstable` 展示原始分并建议人工复核；`escalated` 元数据如实转述。',
  '- 合并版必须过门禁（分数不低于冠军，噪声带内）才可取代冠军，否则回退并说明原因。',
  '',
  '---',
  '',
  '*本说明由 usage-a 整合合并 usage-a / usage-b / usage-c 三份独立实现而成（Best-of-N 演练合并产物）。*',
].join('\n');

// 打印完整说明（末尾留一行空白便于阅读）
console.log(MD);
console.log('');

// 冒烟测试要求：以 0 退出码正常结束
process.exit(0);
